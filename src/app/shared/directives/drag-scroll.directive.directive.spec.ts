import { DragScrollDirectiveDirective } from './drag-scroll.directive.directive';

describe('DragScrollDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DragScrollDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
