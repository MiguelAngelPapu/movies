import { InfiniteScrollDirectiveDirective } from './infinite-scroll.directive.directive';

describe('InfiniteScrollDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new InfiniteScrollDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
