import { OnlyNamesGenrePipe } from './only-names-genre.pipe';

describe('OnlyNamesGenrePipe', () => {
  it('create an instance', () => {
    const pipe = new OnlyNamesGenrePipe();
    expect(pipe).toBeTruthy();
  });
});
